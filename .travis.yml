language: go
go:
  - 1.7

# Sudo is required for docker
sudo: required

# Enable docker
services:
  - docker

before_install:
  - go get github.com/mattn/goveralls
script:
  - $GOPATH/bin/goveralls -service=travis-ci
